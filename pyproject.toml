[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fram-e2e-safety"
version = "0.1.0"
description = "Small reproducible FRAM analysis for an autonomous driving case study"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
